{
  "name": "pastemorphbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "turbo dev --filter=@pastemorphbox/web",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "preview": "pnpm --filter @pastemorphbox/web preview"
  },
  "devDependencies": {
    "turbo": "^2.7.0",
    "typescript": "~6.0.2",
    "vitest": "^4.0.16"
  }
}
