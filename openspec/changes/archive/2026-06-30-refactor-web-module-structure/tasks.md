## 1. OpenSpec Artifacts

- [x] 1.1 Create proposal, design, and task artifacts for the web module split.

## 2. Web Source Layout

- [x] 2.1 Create the `modules/paste-workbench` module with focused component directories.
- [x] 2.2 Move smart input, examples, empty/no-match states, result list, tool card, field row, and editable value into separate files.
- [x] 2.3 Keep `App.tsx` as a small shell and `app/` as route-only entry points.
- [x] 2.4 Move the input store into `store/input-store.ts` and update imports.

## 3. Verification

- [x] 3.1 Run `pnpm test`.
- [x] 3.2 Run `pnpm typecheck`.
- [x] 3.3 Run `pnpm build`.
