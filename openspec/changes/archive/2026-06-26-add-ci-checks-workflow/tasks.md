## 1. CI Workflow

- [x] 1.1 Add a GitHub Actions workflow for pull requests and pushes to `main`.
- [x] 1.2 Configure the workflow to use Node.js 24 and `pnpm@11.9.0`.
- [x] 1.3 Configure dependency installation with `pnpm install --frozen-lockfile`.

## 2. Checks

- [x] 2.1 Run `pnpm test` in CI.
- [x] 2.2 Run `pnpm typecheck` in CI.
- [x] 2.3 Run `pnpm build` in CI.

## 3. Verification

- [x] 3.1 Run `pnpm test` locally.
- [x] 3.2 Run `pnpm typecheck` locally.
- [x] 3.3 Run `pnpm build` locally.
