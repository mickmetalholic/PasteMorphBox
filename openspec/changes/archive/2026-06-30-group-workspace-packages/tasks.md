## 1. OpenSpec Artifacts

- [x] 1.1 Create proposal, design, and task artifacts for workspace package grouping.

## 2. Workspace Layout

- [x] 2.1 Move shared packages into `packages/shared`.
- [x] 2.2 Move tool packages into `packages/tools`.
- [x] 2.3 Move registry into `packages/platform`.
- [x] 2.4 Move config packages into `packages/config`.
- [x] 2.5 Update workspace globs for nested package directories.
- [x] 2.6 Update nested package script paths that depend on repository-relative locations.

## 3. Verification

- [x] 3.1 Run `pnpm test`.
- [x] 3.2 Run `pnpm typecheck`.
- [x] 3.3 Run `pnpm build`.
