## 1. OpenSpec Artifacts

- [x] 1.1 Create proposal, design, and task artifacts for the registry split.

## 2. Registry Source Split

- [x] 2.1 Move static tool registration into `modules.ts`.
- [x] 2.2 Move example aggregation and discovery helpers into `examples.ts`.
- [x] 2.3 Move derived input candidate decoding into `derived-candidates.ts`.
- [x] 2.4 Move dedupe and ranking policy into `ranking.ts`.
- [x] 2.5 Keep `index.ts` as the stable public API surface.

## 3. Verification

- [x] 3.1 Run `pnpm test`.
- [x] 3.2 Run `pnpm typecheck`.
- [x] 3.3 Run `pnpm build`.
