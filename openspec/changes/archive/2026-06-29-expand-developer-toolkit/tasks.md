## 1. Package Setup

- [x] 1.1 Create `packages/tool-developer` package, tsconfig, and package scripts.
- [x] 1.2 Add the package to the workspace lockfile/package graph as needed.

## 2. Developer Tool Implementation

- [x] 2.1 Implement JWT, Base64URL, UUID, hash-shaped, and HTML entity detection.
- [x] 2.2 Implement decoded/encoded fields for each supported developer value kind.
- [x] 2.3 Add developer tool discovery metadata and examples.

## 3. Registry Integration

- [x] 3.1 Register the developer tool with existing tools.
- [x] 3.2 Update registry tests for display order and example detection.

## 4. Tests

- [x] 4.1 Add detector and converter tests for developer utility behavior.
- [x] 4.2 Run `pnpm test`, `pnpm typecheck`, and `pnpm build`.
