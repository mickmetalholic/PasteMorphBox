## 1. Input Privacy

- [x] 1.1 Stop writing smart input changes into the URL query by default.
- [x] 1.2 Clear stale `q` query params on explicit input updates.
- [x] 1.3 Update status/privacy copy for local URL behavior.

## 2. Card Workflows

- [x] 2.1 Add per-card bulk copy for field labels and values.
- [x] 2.2 Fold result cards with more than six fields and add expand/collapse control.
- [x] 2.3 Preserve existing per-field copy and apply-to-input behavior.

## 3. Verification

- [x] 3.1 Run `pnpm test`, `pnpm typecheck`, and `pnpm build`.
