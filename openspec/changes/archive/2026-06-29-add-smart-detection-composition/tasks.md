## 1. Registry Composition

- [x] 1.1 Add derived candidate extraction for percent decoding, URL params, Base64/Base64URL, and JWT payloads.
- [x] 1.2 Run registered detectors against direct and derived candidates with derived result labeling.
- [x] 1.3 Deduplicate direct and derived matches by tool and source.

## 2. Tests

- [x] 2.1 Add registry tests for percent-decoded JSON, encoded JSON, and JWT payload composition.
- [x] 2.2 Run `pnpm test`, `pnpm typecheck`, and `pnpm build`.
