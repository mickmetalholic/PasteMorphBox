# JSON Tool Spec

## Requirements

### Requirement: JSON formatting

The JSON tool SHALL validate, format, and compact JSON input.

#### Scenario: Valid object

- **WHEN** the user enters compact JSON
- **THEN** the app SHALL show formatted and compact JSON fields.
